{% extends 'cms_base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col">
                <h1>Add movie</h1>
                {{  form_start(MovieFormType) }}
                    {{ form_widget(MovieFormType) }}
                    <button type="submit" class="btn btn-primary" formnovalidate>Add</button>
                {{ form_end(MovieFormType) }}
            </div>
        </div>
    </div>

{% endblock %}